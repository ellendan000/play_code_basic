spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
      url: "jdbc:h2:mem:play_code_basic;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_UPPER=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS play_code_basic\\;SET SCHEMA play_code_basic"